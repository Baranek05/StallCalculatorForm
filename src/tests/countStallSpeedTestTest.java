package tests;

import counting.CountStallSpeed;
import data.AngleOfAttack;
import data.IPlane;
import data.Plane;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class countStallSpeedTestTest {
    /*@Test
    public void tester(){
        IPlane plane = new Plane();
        AngleOfAttack aoa = new AngleOfAttack();
        CountStallSpeed count = new CountStallSpeed();
        plane.setWeight(38328);
        plane.setSurface(122.6);
        aoa.setAngleOfAttack(15);
        assertEquals(128.0399276155, count.countStallSpeed(plane, aoa, 2365, 0.9));
    } */
}